---
description: "Phase 1: Scaffold a landing page with plain CSS based on openspec context"
---

Launch the **ui-designer** agent to execute **Phase 1: Structure — Plain CSS** from the 3-phase bootstrap.

> Execute Phase 1 from the "Initial setup — 3-phase bootstrap" section in your agent instructions.
>
> Build an Angular landing page with plain CSS. Read `openspec/project.md` for project context. No Tailwind, no component libraries.
>
> When done, verify the build and ask the user to commit.
>
> $ARGUMENTS
